/**
 * TutorsLink Firebase Configuration
 *
 * IMPORTANT: Replace ALL placeholder values below with your real Firebase
 * project credentials from the Firebase console:
 *   https://console.firebase.google.com → Project settings → Your apps → Web app
 *
 * DO NOT commit real credentials to version control.
 * Store them in environment variables or use Firebase App Hosting environment config.
 *
 * To load via Firebase Functions config (for Cloud Functions):
 *   firebase functions:config:set discord.bot_token="YOUR_BOT_TOKEN"
 *   Access in functions: functions.config().discord.bot_token
 *
 * See docs/discord-integration.md for Discord-specific setup.
 */
var FIREBASE_CONFIG = {
  apiKey:            "REPLACE_WITH_YOUR_API_KEY",
  authDomain:        "REPLACE_WITH_YOUR_AUTH_DOMAIN",
  projectId:         "REPLACE_WITH_YOUR_PROJECT_ID",
  storageBucket:     "REPLACE_WITH_YOUR_STORAGE_BUCKET",
  messagingSenderId: "REPLACE_WITH_YOUR_MESSAGING_SENDER_ID",
  appId:             "REPLACE_WITH_YOUR_APP_ID"
};
